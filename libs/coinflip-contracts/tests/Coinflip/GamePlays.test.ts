import { ethers } from 'hardhat';
import { expect } from 'chai';

describe('GamePlays', () => {
  describe('createGamePlay', () => {
    it('creates game play', async () => {
      console.log('Runs create game play test');
    });
  });
});
